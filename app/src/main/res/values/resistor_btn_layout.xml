<!-- res/values/styles.xml ou themes.xml -->
<resources>
    <!-- Outros estilos -->
    <style name="CalculadoraBotaoNumero" parent="Widget.MaterialComponents.Button.OutlinedButton">
        <item name="android:layout_width">70dp</item> <!-- Ou um tamanho fixo se preferir -->
        <item name="android:layout_height">wrap_content</item>
        <item name="android:minWidth">48dp</item> <!-- Tamanho mínimo recomendado para toque -->
        <item name="android:minHeight">48dp</item>
        <item name="android:layout_marginTop">0dp</item>
        <item name="android:layout_marginBottom">0dp</item>
        <item name="android:padding">1dp</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:shadowColor">@android:color/black</item>
        <item name="android:shadowDx">3</item>
        <item name="android:shadowDy">3</item>
        <item name="android:shadowRadius">1.5</item>
        <!-- Você pode adicionar app:backgroundTint aqui se quiser uma cor de fundo específica -->
        <!-- Ex: <item name="backgroundTint">@color/sua_cor_botao</item> -->
        <!-- Se usar OutlinedButton, você pode querer estilizar a borda com app:strokeColor -->
    </style>
</resources>
